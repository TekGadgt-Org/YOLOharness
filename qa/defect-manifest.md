# Phase 1 WRC defect manifest

Status: retained acceptance-planning artifact; this file does not waive an open row or claim release approval.

Scope and provenance

- Source checklist: `qa/remediation-checklist.md` (WRC closure list and finding reconciliation).
- Source matrix: `qa/phase1-acceptance-matrix.md` (current Linux milestone status).
- Source inventory: `qa/wrc-finding-test-inventory.md` (original WRC-01..21 contract rows, prior probes, and historical results).
- Source disposition: `qa/phase1-consolidated-disposition.md`; detailed shipped evidence: `qa/real-docker-evidence.md` and `qa/wrc-baseline-raw/*`.
- Parent contracts and restrictions named by the inventory are `t_0a30f9b2` (amended task), `t_b8cfa081` (security findings), `t_55bbce54` (restrictions), and attachment `whole-runtime-container-contract.md`. The attachment itself is not present in this checkout; the inventory preserves its WRC-01..21 row text and is used as the row source, with that absence recorded as a source limitation rather than guessed content.
- Execution boundary: Linux 6.8.0-138-generic x86_64, Node v22.23.2, rootless Docker 29.8.0, synthetic credentials/provider only. WRC-21 is deferred by the approved Linux-only milestone; Ryan supplies native macOS evidence after Linux ships.

Operational prohibitions inherited from the parent contracts: no real credentials, live OAuth, canonical external provider requests, host trust changes, privilege changes, chmod/chown or world-writable workarounds, expanded mounts, production policy hooks, production overrides, host execution of model text, global install, publish, push, or modification of unowned Docker resources. Tests must use disposable working directories, test-owned synthetic fixtures/resources, rootless Docker where required, the shipped CLI as an OS subprocess, and exact before/after ownership inventories.

Evidence conventions

- `npm test` and `npm run test:docker` are repository commands; the opt-in equivalent is `YOLO_EVIDENCE_DIR=qa/wrc-baseline-raw YOLO_REAL_DOCKER=1 node --test test/real-docker.test.mjs`.
- Raw command, stdout, stderr, status, and timestamp files live under `qa/wrc-baseline-raw/`; `sh qa/capture-wrc-baseline.sh` refreshes the retained baseline.
- For each proposed gate, capture `git rev-parse HEAD`, engine/daemon facts, disposable cwd, exact CLI argv/env, exit status, expected diagnostic, positive-control result, owned resource IDs/names, and after-run absence. A passing unit or raw Docker probe is not a shipped-CLI pass.

## One row per original WRC identifier

| WRC row | Status / actionability challenge | Supporting evidence and affected boundary | Expected negative probe (reproducible acceptance criterion) | Positive control | Shipped-CLI invocation | Owned resources and exact cleanup |
|---|---|---|---|---|---|---|
| WRC-01 | PASS scoped; actionable for full whole-runtime contract. | `real-docker-evidence.md` provider roundtrip passes in an ephemeral CA-only derivative/internal network; production image selection remains tagged. Boundary: CLI -> image -> provider network. | Inject model-like metacharacters/tool text and assert only fixed Docker argv is used; provider capture must show container origin and no host operation. | Ordinary synthetic provider tool roundtrip returns `whole-runtime-ok`. | `node src/cli.mjs` through the named real-Docker subprocess test `shipped yolo subprocess uses the immutable CA-only derivative and an internal provider network`. | Test-owned derivative, internal network, CA/capture/workspace roots and runtime/provider containers; record exact names before/after and assert all absent, leaving pre-existing resources untouched. |
| WRC-02 | PASS scoped; current emitted-data instrumentation retained. | Fixed argv/parser tests plus provider-only emitted SSE capture exist; affected boundary is launcher stdin/argv and host-execution prevention. | Send a distinct shell metacharacter/host-path payload as provider output; assert it remains data, reaches no host process/filesystem operation or injected Docker command, and unsupported fixture option still fails. | Normal fixed-argv provider/tool roundtrip succeeds. | `node src/cli.mjs --fixture` from disposable cwd must fail `unknown option`; then run ordinary `node src/cli.mjs` positive control via real-Docker test. | Only test-owned runtime/provider fixtures; exact generated runtime/container names absent after both runs. |
| WRC-03 | PASS scoped shipped preflight; full live-daemon/image acceptance remains bounded by the real-Docker gate. | `test/shipped-preflight.test.mjs` invokes the shipped CLI from disposable cwd fixtures and retains unavailable executable, unavailable daemon, absent image, and valid preflight controls. Boundary: CLI preflight/image policy before provider/credentials. | Each negative exits 1 with its intended diagnostic/phase; no `create`/`start`, provider execution, or credential handoff occurs. The daemon case uses a test-owned synthetic credential file only to reach `docker info`. | Valid client/daemon/image wrapper reaches the credential boundary without runtime start; no real credential is used. | `YOLO_REAL_DOCKER=1 node --test test/shipped-preflight.test.mjs`; raw `qa/wrc-baseline-raw/shipped-preflight.command`, `.stdout`, `.stderr`, `.status`. | Test-owned wrapper and disposable fixture root are removed by the test; no Docker resource is created. |
| WRC-04 | PASS scoped shipped inspection; broader platform evidence remains actionable. | `runtime-inspect.stdout` proves one `/workspace` bind and prohibited targets absent. Boundary: Docker create config -> daemon inspection. | Run inspection and assert exactly one writable `/workspace` bind, no home/socket/proc/device/host-secret mount, and no host mode. | Ordinary workspace artifact read/write/delete succeeds through the one bind. | Real-Docker shipped subprocess plus daemon inspection in `test/real-docker.test.mjs`; retain `runtime-inspect.stdout`. | Test-owned runtime/provider/network/workspace; remove exact owned IDs and verify not-found, preserve pre-existing daemon resources. |
| WRC-05 | PASS for launcher boundary; actionable broader final-image sentinel coverage. | Runtime policy rejects parent/root-relative and outside-resolving symlink paths; affected boundary is workspace validation and bind. | From disposable cwd, request `..`, root-relative, absolute outside, and outside-resolving symlink; assert fail closed before launch and outside sentinel unchanged/unreadable. | Nested in-workspace path and in-workspace symlink write/read/delete succeed. | `node src/cli.mjs` shipped subprocess with test-owned workspace/sentinel fixtures; do not treat direct `runtime.test.mjs` alone as closure. | Test-owned cwd, symlink, sentinel and any runtime; remove fixture roots and exact runtime name, verify sentinel content/hash unchanged. |
| WRC-06 | PASS for launcher boundary; actionable broader container-namespace canary. | `src/container-launcher.mjs` rejects outside-resolving symlink; matrix explicitly bounds claim by Linux bind namespace. | Shipped CLI accesses a test-owned symlink resolving outside project; assert rejection and no outside sentinel mutation/read. | Symlink resolving within project behaves as an ordinary workspace path. | Real shipped CLI subprocess from disposable cwd using the named symlink negative/control. | Test-owned project, outside canary, symlink, runtime/network; assert exact resource absence and outside canary unchanged. |
| WRC-07 | PASS scoped escaped-newline control; actionable only for an unprivileged nested-mount fixture. | Mountinfo `\\012` decoding and newline-cwd rejection pass; unprivileged nested mount proof is environment-bounded. Boundary: workspace scan -> Docker mount. | Use test-owned nested-mount or escaped-newline fixture; assert rejection before Docker launch/mount parsing. | Ordinary disposable directory with no nested mount launches and completes. | Shipped `node src/cli.mjs` newline-cwd negative and ordinary-cwd control; retain named parser unit separately. | Test-owned cwd/mount fixture only; unmount/remove only resources created by test, then assert exact Docker inventory unchanged. |
| WRC-08 | PASS unit only; actionable shipped subprocess coverage. | Hardlink/multi-link rejection exists in `test/container-launcher.test.mjs`; boundary is workspace scan. | Create a test-owned multi-link regular file and assert shipped CLI rejects it before container launch. | Single-link regular file and normal project complete. | `node src/cli.mjs` from disposable cwd with hardlink negative/control; do not substitute imported launcher call. | Test-owned files/inodes and any runtime; unlink both aliases, remove cwd, verify no owned Docker resource remains. |
| WRC-09 | PASS scoped shipped control. | Real shipped CLI emits the intentional-exposure warning; provider tool reads synthetic `.env`, key, and token fixtures through the sole `/workspace` bind. Boundary: CLI warning and intentional project exposure. | Synthetic secret fixtures are readable to the agent and warning is explicit; no confidentiality claim is made and unrelated host secret paths are not mounted. | Ordinary provider/tool roundtrip succeeds with the same project bind. | `YOLO_EVIDENCE_DIR=qa/wrc-baseline-raw YOLO_REAL_DOCKER=1 node --test test/real-docker.test.mjs`; capture stdout/stderr and status. | Test-owned project/files/runtime/network; delete fixtures and exact resources, assert no secret bytes in retained logs/evidence. |
| WRC-10 | PASS scoped shipped daemon-observed row. | Rootless UID0, read-only root, tmpfs, workspace canary, and full named runtime inspection pass. Boundary: launcher flags -> daemon/container. | Shipped runtime attempts `/app` write and prohibited rootfs/host mount access; assert rejection, rootless-only policy and expected limits/flags. | `/workspace` write/delete canary succeeds under approved 0:0 mapping; `/tmp` and synthetic-home tmpfs are bounded. | `node src/cli.mjs` via the named real-Docker shipped subprocess; retain `runtime-inspect.stdout` and focused canary. | Test-owned image derivative/runtime/network/workspace; exact owned resources removed and before/after image/network inventories recorded (do not delete installation image). |
| WRC-11 | PASS for asserted fields; actionable daemon-dependent seccomp/exhaustive inspection. | `runtime-inspect.stdout` covers read-only root, dropped caps, NNP, PID/memory/CPU, mounts/network. Boundary: daemon security configuration. | Inspect shipped runtime and assert bounded limits, no capabilities, NNP, no socket/device/host mounts, and expected seccomp/rootless properties. | Below-limit ordinary provider/tool roundtrip remains successful. | Shipped real-Docker test plus `docker inspect` evidence, never raw inspect alone as the full row. | Test-owned runtime/provider/network; stop/remove exact IDs and verify no owned resources remain. |
| WRC-12 | PASS scoped Linux synthetic shipped gate; broader WRC-20 linkage remains actionable. | Started-marker deadline exit 124, delayed write absent, exact owned inventory empty are retained in `real-docker.*`. Boundary: CLI timeout -> descendant lifecycle. | `yolo --json -t 0.05 deadline-probe` after started marker must exit 124, never create late marker, and leave exact owned runtime absent. | Longer-budget same delayed-marker control writes successfully and cleans up. | Invoke `node src/cli.mjs` as child through the shipped real-Docker row; retain stdout/stderr/status/timestamps and inventory. | Test-owned runtime/workspace/network; assert generated name absent after cancellation and late-write path absent; remove only owned fixtures. |
| WRC-13 | PASS scoped Linux synthetic shipped gate; actionable only for other platform coverage. | OS SIGINT after marker exits 130, late marker absent, exact runtime absent. Boundary: OS child signal -> launcher -> container. | Send SIGINT after `/workspace/sigint-started`; assert exit 130, no late descendant write, stable exact absence. | Same command without SIGINT completes and writes its expected marker. | Shipped CLI OS subprocess in real-Docker test; retain signal timing and raw output/status. | Test-owned runtime/workspace/network; stop/remove exact ID/name and verify no late marker and no owned resource. |
| WRC-14 | PASS scoped Linux synthetic shipped gate; actionable only for other daemon/platform boundaries. | Independent stdout-only/stderr-only 1 MiB+ overflow exit 124 and cleanup pass; short-delay controls pass. Boundary: container streams -> collector. | Run stdout-only and stderr-only overflow separately; assert bounded failure, no hang/late write, exact owned absence. | Short-delay stdout-only and stderr-only controls complete and write markers. | Shipped CLI subprocess in `test/real-docker.test.mjs`; retain separate command/status/output for each stream. | Test-owned runtime/workspace/network; exact runtime names absent after each case and output fixtures removed. |
| WRC-15 | PASS scoped; actionable full daemon environment inspection. | Hostile selector/XDG provenance negative and shipped fixture rejection pass; daemon env inspection remains incomplete. Boundary: inherited env -> explicit container env. | Set hostile HOME/XDG/Docker/proxy/cloud/SSH/test selectors; assert image/network/credentials unchanged, prohibited vars absent, and no secret leakage. | Explicit test-owned empty config/local endpoint and normal provider control succeed. | Shipped `node src/cli.mjs` child with controlled env; retain argv/env inspection and hostile/normal outputs. | Test-owned config/home/XDG/workspace/runtime/network; remove exact roots/resources and verify no unowned context/image/network changed. |
| WRC-16A | PASS scoped shipped control. | Same-container shipped bootstrap/provider probe uses a synthetic access token; runtime argv/env/logs omit token material and a provider 401 maps to `reauth_required`. Boundary: bootstrap stdin -> provider. | Exactly one synthetic 401 fails closed without retry or in-container refresh; refresh token never enters the container or retained output. | Synthetic valid access-token provider roundtrip succeeds once; no-refresh control remains absent. | Shipped real-Docker CLI subprocess in `test/real-docker.test.mjs`; no real credentials. | Test-owned token/config/provider/runtime/network; assert token absence from evidence and exact owned cleanup. |
| WRC-16B | NON-ACTIONABLE / INAPPLICABLE, challenged and evidence-backed. | Approved amended contract selects Option A/WRC-16A; implementing a relay would be unrelated scope and duplicate the selected boundary. No relay probe is required or claimed. | No acceptance negative is applicable; if contract changes, create a new explicitly approved Option B row rather than silently reclassifying this one. | N/A under Option A. | No shipped invocation by design; record amendment/contract reference. | No WRC-16B resources are created; assert no relay/socket resource exists as part of normal cleanup. |
| WRC-17 | PASS offline synthetic; applicable live-credential claims prohibited. | Auth rotation/atomic persistence and multi-line reopen controls pass in tests; boundary is host AuthStore/event log. | Concurrent/crash/old-generation write must not overwrite new generation; container cannot write credential store; malformed multi-line reopen must not corrupt append. | Fresh auth generation and ordinary append/reopen sequence succeeds. | `node src/cli.mjs` shipped offline where applicable, plus `npm test`/focused integration tests; no real credentials. | Test-owned HOME/XDG/auth/event-log/workspace; remove exact temporary roots and verify no credential bytes remain. |
| WRC-18 | PASS scoped shipped setup probe. | The current focused shipped `yolo setup` probe captures the actual generated package/src context and secret-free image history/export; derivative-only evidence remains historical. Boundary: build context -> image layers. | Build/export/inspect history and layers; assert synthetic secrets are absent and no secret-valued build metadata is present. | Allowlisted runtime/image artifact builds and remains usable by the shipped provider control. | Current focused shipped setup plus historical derivative evidence. | Test-owned build context/image tag/export/temp roots; remove only test-created resources and preserve installation-owned image. |
| WRC-19 | PASS scoped; actionable exhaustive executable inventory. | Shipped env/mount inspection finds no Docker socket/nested Docker and declared tool control works; exhaustive inventory bounded. Boundary: runtime namespace/tool allowlist. | Inspect shipped runtime for socket/CLI paths, undeclared executables, host mounts and nested daemon access; assert absent/fail closed. | Declared provider/tool command succeeds. | Shipped real-Docker CLI provider roundtrip plus runtime inspection. | Test-owned runtime/provider/network/workspace; exact owned cleanup and pre-existing resource preservation. |
| WRC-20 | ACTIONABLE aggregation/evidence row; explicitly PARTIAL. | Current rootless daemon/image/source linkage, exact-owned cleanup, and applicable Linux row evidence are reconciled; WRC-07 remains environment-bounded and WRC-21 deferred. Boundary: release evidence integrity. | Reconcile every applicable row to the frozen source candidate/image label/immutable ID and retain raw command/status/output plus before/after inventory; reject publication if any row is partial/unrun. | Current rebuilt installation-owned image, rootless daemon, applicable row set, and empty owned post-run inventory. | `git rev-parse HEAD`; `docker info --format '{{json .}}'`; `docker image inspect --format '{{json .}}' yoloharness-local:0.1.0`; `sh qa/capture-wrc-baseline.sh`; all focused shipped CLI gates. | Capture inventories before/after; delete only test-owned derivative/network/runtime/provider/temp roots; assert exact owned absence and preserve pre-existing `objective_chatelet`/installation image. |
| WRC-21 | NON-ACTIONABLE for this Linux milestone but deferred, not passed. | Approved Linux-only amendment explicitly assigns native macOS Docker Desktop evidence to Ryan after Linux ships. Observed host is Linux; no macOS result exists. | No Linux substitute is valid. Native macOS run must execute the equivalent matrix and record engine/device/output/status; until then classify DEFERRED. | Native macOS Docker Desktop full applicable matrix, after Linux milestone, is the only positive control. | None in this environment; do not fabricate a macOS command or status. | Ryan's native run owns only its disposable fixtures; Linux worker owns none for WRC-21. Exact cleanup must be reported by the native run.

## Contradictions and missing source material

1. `qa/wrc-finding-test-inventory.md` retains historical/stale statuses for WRC-05/06 and WRC-12..14, while the newer checklist/matrix and raw evidence record shipped symlink/lifecycle controls. The historical values must remain historical; current acceptance uses the newer named evidence, not a silent overwrite.
2. The matrix labels WRC-03 `FAIL`; the checklist calls it `PARTIAL`; the inventory calls its offline subset `PASS` while shipped negatives are `NOT RUN`. This manifest normalizes the meaning as actionable residual, not a release failure caused by a product regression and not a pass.
3. The checklist line describing WRC-12 as offline-only and the line saying OLD-05 remains are stale relative to the later retained deadline/SIGINT/overflow evidence. The later evidence closes scoped WRC-12..14 but does not close WRC-20 aggregation.
4. Older retained documents report 84/86, 85/87, or 86/88. The current verified baseline is 88 passed, 0 failed, 9 skipped, 97 total (`npm test`); historical totals are not current results.
5. The referenced `whole-runtime-container-contract.md` and live parent-task records are not files in this worktree. The inventory and Kanban handoffs are the available transcriptions; any omitted contract clause must be surfaced for human review, not inferred.
6. WRC-21 is intentionally neither passed nor failed: Linux evidence cannot substitute for native macOS Docker Desktop evidence.

Manifest completeness check: exactly one row is present for each identifier WRC-01 through WRC-21; WRC-16A and WRC-16B are separate original identifiers. This artifact records the shipped warning/error-semantics changes and test evidence; it does not itself approve release.
