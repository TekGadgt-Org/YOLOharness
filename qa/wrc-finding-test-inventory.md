# WRC finding-to-test inventory and baseline reproduction

Status: final remediation evidence for the current feature head; production corrections and shipped runtime inspection probes are included.

Authoritative inputs: amended task t_0a30f9b2, security findings t_b8cfa081, restrictions t_55bbce54, and verified contract attachment `whole-runtime-container-contract.md` (WRC-01..21). Amendments are applied: WRC-16A is applicable (Option A); WRC-16B is inapplicable; WRC-21/native macOS is explicitly NOT RUN. Historical rejected candidate: `c7672b392e484493c88fe8d8b555604f017de59d`; the current branch was independently reproduced at `5d62c13`. Historical image/source identifiers are retained in the historical rows below; current local image inspection is retained in `qa/wrc-baseline-raw/image-inspect.stdout`.

## Current retained Linux run

The historical rows below are preserved for traceability and are not the current candidate ledger. Current implementation checkout is HEAD `3f2ffaa85aae689bf568765cfb463fc273caa82a`; immutable production image is `sha256:7d40fa5f991fc167366b31f082c1a9ee5c2e3ca779f8d69e1a997fcf78f58f08` with source label `sha256:fad13e8ac9129acf775b6f5b11624929872d32108be657312cadf204490ca122`. The fresh rootless gate passed 3/3 and retains named shipped subprocess artifacts for WRC-05/06 (`wrc-05-06-boundary.*`), WRC-08 (`wrc-08-negative.*`, `wrc-08-control.*`), WRC-10/11 (`wrc-10-11-resource.*`), and WRC-19 (`wrc-19-nested-docker.*`). WRC-18 shipped setup is independently retained as `wrc-18-setup.*` and passes the actual package/src context and layer scan. WRC-21 remains deferred by the approved Linux-only milestone. Historical cleanup failure remains separately attributable in `qa/remediation-checklist.md` to attach-pipe cleanup racing daemon removal; current immediate exact-owned cleanup controls pass.

All commands below run from the repository root unless noted. Raw stdout, stderr, exit status, and ISO timestamps are retained as paired files under `qa/wrc-baseline-raw/`; capture script: `sh qa/capture-wrc-baseline.sh`. The script uses only synthetic disposable paths and does not read real auth/config. `npm test` is the repository command; `npm run test:docker` is the configured rootless real-Docker command. The real-Docker run created only test-owned derivative/network fixtures and its test cleanup removed them; before/after inventories are retained.

| ID | Requirement/finding and negative probe | Production boundary traversed | Exact test name and path | Exact command / retained raw evidence | Expected assertion and positive control | Prior observed result (before this reproduction) | Baseline result / missing assertion | Applicability rationale |
|---|---|---|---|---|---|---|---|---|
| WRC-01 | Whole provider/planner/runtime is containerized; synthetic provider only internal network | shipped CLI -> fixed image -> provider network | `shipped yolo subprocess uses the immutable CA-only derivative and an internal provider network` (`test/real-docker.test.mjs:92`) | `npm run test:docker`; `real-docker.*` | Provider capture has nonce/container origin and tool roundtrip; control is image canary | PASS scoped derivative only | FAIL: provider returned synthetic 401 before capture (`real-docker.stdout:29-37`); no successful whole-runtime roundtrip | Applicable Linux shipped-runtime gate |
| WRC-02 | Host never executes model text or turns it into host operations | CLI subprocess -> Docker argv/stdin | same test; offline parser controls in `test/integration.test.mjs` | `npm test`; `npm-test.*`, `real-docker.*` | Host performs only fixed Docker launch; malicious metacharacters are data; test-only trace records child/filesystem calls and control starts fixed argv | PASS scoped | Synthetic provider payload, host trace, fixed Docker argv, and unchanged canary are retained in `wrc-02-provider-payload.txt` and `wrc-02-host-trace.jsonl`; instrumentation scope is test-only, not kernel-wide | Applicable |
| WRC-03 | Missing executable, unavailable daemon, absent image fail closed | CLI preflight/image policy -> Docker | `default CLI fails closed with setup guidance when runtime image is unavailable` (`test/integration.test.mjs`); shipped empty fixture | `env -i PATH=/usr/bin:/bin HOME=/tmp/yoloharness-baseline-home ... node src/cli.mjs 'synthetic baseline probe'`; `shipped-empty.*` | Nonzero before provider/model and no host fallback; valid configured image control is the real-Docker test | FAIL/absent on rejected candidate | PASS for missing metadata: `no model configured` exit 1, but unavailable executable/daemon/absent immutable image shipped probes remain NOT RUN | Applicable |
| WRC-04 | Exactly one `/workspace` bind; no home/socket/proc/device/host modes | shipped runtime inspection and generated `boundary-probe` in `test/real-docker.test.mjs` | `runtime-inspect.stdout`; `wrc-05-06-boundary.*` | Exactly one bind and prohibited targets absent; `/workspace` sentinel control | PASS scoped | Named daemon and in-container absence probes pass; exhaustive host namespace claims excluded | Applicable |
| WRC-05 | Parent/absolute paths cannot escape project | workspace validation -> bind -> runtime filesystem | shipped `boundary-probe` in `test/real-docker.test.mjs` | `wrc-05-06-boundary.*` | Outside sentinels unreadable/unchanged; normal `/workspace` artifact control | PASS scoped | Named shipped boundary probe passes; broader image claims excluded | Applicable |
| WRC-06 | Symlink namespace does not expose outside host sentinel; container-target control documented | host bind -> container namespace | shipped `boundary-probe` in `test/real-docker.test.mjs` | `wrc-05-06-boundary.*` | Outside symlink probe fails/does not alter sentinel; known container symlink control behaves normally | PASS scoped | Named shipped boundary probe passes; broader image claims excluded | Applicable |
| WRC-07 | Nested bind mount rejected or proven non-recursive | workspace validation -> Docker mount | `workspace validation rejects nested mount points and multiply-linked files` (`test/container-launcher.test.mjs`) | `npm test`; `npm-test.*` | Nested fixture rejected; ordinary directory positive control | IMPLEMENTED only | PARTIAL: unit path exists, no unprivileged nested-mount shipped fixture/daemon inspection | Applicable Linux gate |
| WRC-08 | Hardlink aliases rejected | workspace scan -> bind | nested `WRC-08 shipped CLI rejects a hardlink alias before model execution` in `test/real-docker.test.mjs`; unit regression in `test/container-launcher.test.mjs` | `YOLO_EVIDENCE_DIR=qa/wrc-baseline-raw YOLO_REAL_DOCKER=1 node --test test/real-docker.test.mjs`; `real-docker.*` | Shipped CLI rejects multi-link regular file before provider/model execution, outside sentinel remains unchanged and no provider request is added; same-runtime ordinary single-link control completes | PASS unit | PASS shipped subprocess negative/control plus unit | Applicable |
| WRC-09 | Project secrets are intentionally exposed and warning is explicit; no confidentiality claim | CLI warning -> `/workspace` | shipped warning/readability control in `test/real-docker.test.mjs` | `npm run test:docker`; `real-docker.*` | Synthetic `.env`/key/token readable with warning; ordinary file control readable | PASS scoped Linux synthetic provider | Shipped CLI emits warning and provider tool reads test-owned fixtures through `/workspace`; no confidentiality claim | Applicable |
| WRC-10 | Rootless-only UID mapping, read-only root, tmpfs, writable project | launcher flags -> daemon/container | `configured final image has read-only root and rootless UID0 workspace write/delete canary` (`test/real-docker.test.mjs:94`) | `npm run test:docker`; `real-docker.*` | UID/rootfs/write-delete canary; provider control is separate | PARTIAL | PASS canary (1/2): `/app` write rejected and workspace canary removed; approved rootless UID0 exception, but no complete shipped runtime row | Applicable; amended contract approves verified rootless UID/GID 0:0 |
| WRC-11 | Resource/security flags enforced by daemon | create config -> daemon inspect/runtime | final-image focused test plus shipped runtime inspection in `test/real-docker.test.mjs` | `wrc-11-focused.*`; `runtime-inspect.stdout`; `wrc-11-shipped-memory-pressure.stdout` | Inspect memory/CPU/PID/caps/NNP/mounts/network; bounded pressure and below-limit controls | PASS scoped | Shipped PID pressure records limit 128 and fork denial; 700 MiB heap pressure records child status 137 and cgroup `oom_kill 1`; runtime probe records Seccomp 2 and NoNewPrivs 1. | Applicable |
| WRC-12 | Deadline kills descendants and prevents late write | shipped CLI -> exact container lifecycle | shipped deadline probe in `test/real-docker.test.mjs` | `real-docker.*` | Started marker, deadline status, exact absence and no late marker; short-delay control writes late marker | PASS scoped | Named shipped deadline/late-write gate passes; historical race remains unresolved attribution | Applicable |
| WRC-13 | Real OS SIGINT cleanup | OS child -> host launcher -> container | shipped subprocess SIGINT/no-late-write and same-command no-SIGINT completion controls | `npm run test:docker`; `real-docker.*` | SIGINT after started marker returns 130; descendants/late marker absent; exact owned runtime name absent; no-SIGINT control reaches delayed marker | PASS scoped Linux synthetic provider | `real-docker.*` | Applicable |
| WRC-14 | stdout/stderr independently bounded | container streams -> host collector | shipped subprocess independent stream controls | `npm run test:docker`; `real-docker.*` | Independent stdout-only/stderr-only overflow cleanup and short-delay controls | PASS scoped Linux synthetic provider | `real-docker.*` | Applicable |
| WRC-15 | Hostile inherited env cannot alter image/network/credentials or leak secrets | inherited env -> explicit container env | `ordinary runtime ignores inherited image and test-control environment` (`test/integration.test.mjs`); shipped probes | `npm test`; `npm-test.*`; `shipped-fixture.*` | Hostile HOME/XDG/DOCKER/proxy/cloud/SSH/test vars absent; explicit allowlist control | PARTIAL | Offline pass; shipped `--fixture` rejection reproduced (`unknown option`, exit 1), but daemon argv/env inspect row absent | Applicable |
| WRC-16A | Option A: only access token handoff; refresh absent; 401 fail closed and exposure honest | host credential helper -> bootstrap stdin -> container provider | shipped access/401 control in `test/real-docker.test.mjs`, plus provider/auth tests | `npm test`; `npm-test.*`, `real-docker.*` | Synthetic provider control; refresh never in container; 401 -> `reauth_required`; inspect argv/env/mount/log | PASS scoped Linux synthetic provider | Shipped row proves stdin bootstrap, token non-disclosure in runtime config/output, and 401 fail-closed receipt; no real credentials | Applicable per amended decision; synthetic tokens only |
| WRC-16B | Option B relay abuse/limits | container -> host relay | None | Not run | Relay controls are irrelevant when Option A is selected | N/A | INAPPLICABLE, intentionally not run | Amendment explicitly selects WRC-16A/Option A |
| WRC-17 | One host refresh owner, atomic rotation/persistence | AuthStore -> atomic file persistence | refresh concurrency/crash tests (`test/integration.test.mjs`) | `npm test`; `npm-test.*` | New generation wins; old cannot overwrite; container cannot write credentials | PASS existing integration | PASS offline synthetic tests; no real credentials used | Applicable offline synthetic only |
| WRC-18 | Allowlisted build context and secret-free layers | setup -> Docker build context/layers | `WRC-18 shipped setup uses only the package/src build context and emits secret-free layers` in `test/real-docker.test.mjs` | `wrc-18-setup.command`, `.stdout`, `.stderr`, `.status` | Actual setup context contains only package/src; synthetic secrets absent from history/export; production tag preserved | PASS scoped shipped setup | Current shipped setup subprocess and run-owned image scan pass; broader supply-chain attestations remain bounded | Applicable |
| WRC-19 | No Docker/Podman socket or nested Docker; declared tools only | runtime container -> tool namespace | shipped `nested-docker-probe` in `test/real-docker.test.mjs` | `real-docker.*`; `wrc-19-nested-docker.*` | Socket/CLI paths absent; ordinary declared tool positive control | PASS scoped | Named generated Docker/Podman attempts fail closed and `sh` control succeeds; exhaustive inventory remains unrun | Applicable |
| WRC-20 | Linux rootless evidence tied to exact source/image/rows | daemon + image -> QA evidence | `qa/wrc-baseline-raw/*`; `qa/real-docker-evidence.md` | `git rev-parse HEAD`; Docker info/image inspect; runtime inspection and named row artifacts | Immutable ID, source label, Docker/kernel/cgroup/rootless evidence and applicable WRC results retained | PARTIAL | WRC-07 remains environment-bounded; WRC-20 is not a release approval | Applicable Linux gate |
| WRC-21 | Native macOS Docker Desktop equivalent | macOS host -> Docker Desktop | None | NOT RUN | Separate macOS evidence required; Linux is not reused | NOT RUN | NOT RUN by explicit policy | Explicitly excluded platform gate |

## Reproduced rejected findings

1. Historical full WRC acceptance gap: an earlier `npm run test:docker` execution returned 1/2 (the canary passed; shipped provider row failed). That result is retained as a failing baseline; the current gate is separately recorded and must not be conflated with it.
2. Shipped bypass probe: the actual CLI subprocess from an isolated disposable environment rejected `--fixture` with `unknown option: --fixture` (exit 1), confirming the latest candidate removed the prior shipped fixture fallback. The historical finding remains recorded because the rejected candidate exposed it.
3. Missing setup state: the actual CLI subprocess from an isolated disposable cwd failed closed with `no model configured` (exit 1), before image/provider execution.
4. Current image/daemon: Docker inspection is retained; daemon evidence is rootless Docker 29.8.0 with cgroup v2 and kernel 6.8.0-138-generic. The current local configured image is immutable and has fixed entrypoint/source label, but differs from the historical rejected image and is not a substitute for full WRC evidence.

## Historical deadline attribution

The original deadline failure is retained as a historical provider-row failure: its exact candidate/image and observed runtime identity remain in `qa/cli-verification-report.md`, where the failure occurred during shipped deadline cleanup. The later deterministic `uncertain create ... delayed daemon appearance` regressions in `test/container-launcher.test.mjs` exercise delayed create reconciliation with no returned ID, a different phase and fixture; they pass and do not establish that they caused the historical provider-row failure. Current deadline/SIGINT runs separately verify started-marker synchronization, no late write, and exact cleanup. Therefore the historical root cause remains irrecoverably unproven, while the current cleanup guarantee is verified separately; no causal claim is made.

Remediation run (current feature head): `test/real-docker.test.mjs` now resolves the already-selected local daemon endpoint before redirecting HOME/XDG, creates a test-owned empty Docker config, and passes only that config plus the verified endpoint to the shipped subprocess. The provider test keeps production metadata bound to the tagged base image; its external test wrapper rewrites only the create image/network for the approved CA-only internal mock. The current row additionally retains WRC-09 warning/readability, WRC-16A access/401, and WRC-18 history/export scans. `configuredImage()` also requires the installation image tag in daemon inspection, so an untagged hostile-XDG derivative cannot satisfy the shipped provenance check. WRC-20 remains partial as recorded above.

No real credentials, live OAuth, remote provider, host trust change, privilege change, chmod/chown, global install, push, publish, or installer modification was used. WRC-16A same-container exposure is accepted as a documented residual risk; WRC-16B and WRC-21 are not run for the stated reasons.
